<nav
  class="bg-background text-foreground shadow-md transition-colors duration-200"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo -->
      <div class="flex-shrink-0">
        <a href="/" class="text-xl font-bold">YellowBaba</a>
      </div>

      <!-- Desktop Menu -->
      <div class="hidden md:flex space-x-4">
        <a href="/" class="text-muted-foreground hover:text-primary transition"
          >Home</a
        >
        <a
          href="/about"
          class="text-muted-foreground hover:text-primary transition">About</a
        >
        <a
          href="/blog"
          class="text-muted-foreground hover:text-primary transition">Blog</a
        >
        <a
          href="/contact"
          class="text-muted-foreground hover:text-primary transition">Contact</a
        >
      </div>

      <!-- Theme Toggle and Mobile Menu -->
      <div class="flex items-center space-x-4">
        <button
          id="themeToggle"
          class="p-2 rounded-md bg-secondary hover:bg-secondary/80 transition-colors"
          aria-label="Toggle theme"
        >
          <svg
            id="sun"
            class="w-5 h-5 hidden dark:block"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707"
            ></path>
          </svg>
          <svg
            id="moon"
            class="w-5 h-5 block dark:hidden"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"
            ></path>
          </svg>
        </button>

        <button
          id="mobileMenuBtn"
          class="md:hidden p-2 rounded-md bg-secondary hover:bg-secondary/80 transition-colors"
          aria-label="Menu"
        >
          <svg
            class="w-5 h-5"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16m-7 6h7"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobileMenu"
    class="hidden md:hidden bg-background border-t border-border"
  >
    <a href="/" class="block px-4 py-2 text-muted-foreground hover:bg-secondary"
      >Home</a
    >
    <a
      href="/about"
      class="block px-4 py-2 text-muted-foreground hover:bg-secondary">About</a
    >
    <a
      href="/blog"
      class="block px-4 py-2 text-muted-foreground hover:bg-secondary">Blog</a
    >
    <a
      href="/contact"
      class="block px-4 py-2 text-muted-foreground hover:bg-secondary"
      >Contact</a
    >
  </div>
</nav>

<script>
  // Theme toggle functionality
  const theme = (() => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  })();

  document.documentElement.classList.toggle("dark", theme === "dark");
  localStorage.setItem("theme", theme || "dark");

  document.getElementById("themeToggle")?.addEventListener("click", () => {
    document.documentElement.classList.toggle("dark");
    localStorage.setItem(
      "theme",
      document.documentElement.classList.contains("dark") ? "dark" : "light"
    );
  });

  // Mobile menu functionality
  const mobileMenuBtn = document.getElementById("mobileMenuBtn");
  const mobileMenu = document.getElementById("mobileMenu");

  mobileMenuBtn?.addEventListener("click", () => {
    mobileMenu?.classList.toggle("hidden");
  });
</script>
